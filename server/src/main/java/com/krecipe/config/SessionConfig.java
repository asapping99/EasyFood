package com.krecipe.config;

/**
 * SessionConfig.java 파일 삭제됨
 * 
 * Spring Session 의존성이 필요하지 않으므로 이 파일은 더 이상 사용되지 않습니다.
 * application.properties의 세션 설정만으로 충분합니다.
 * 
 * 설정 위치: server/src/main/resources/application.properties
 * - server.servlet.session.timeout
 * - server.servlet.session.cookie.*
 * 
 * 이 파일은 수동으로 삭제하셔도 됩니다.
 */
